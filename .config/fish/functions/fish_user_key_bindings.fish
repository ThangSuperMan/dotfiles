function fish_user_key_bindings
  # Peco
  # Notes: \cr -> Control r
  # bind \cr peco_select_history # Bind for peco select history to Ctrl+R
  #bind \cf cd_with_fzf # Search and jum directly to the folder */
  # bind \cf peco_change_directory # Search and jum directly to the folder

  # Vim-like
  # bind \cl forward-char
  
  # fzf_key_bindings
  # bind \cr 'fzf-history-widget'
end
